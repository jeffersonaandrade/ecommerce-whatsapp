# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# Env por cliente (secrets — nunca versionar)
deploy/clients/*/.env.local
deploy/clients/*/.env.production
deploy/clients/*/*.env
!deploy/clients/*/env.example
.env.local
.env.production
.env.local.backup
deploy/clients/*/.env.local.backup

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# graphify (dev tool — mapa versionável; custos locais não)
graphify-out/cost.json
graphify-out/cache/

# IDE / agent local
.claude/
.agents/
.serena/
.mcp.json
skills-lock.json
.cursor/mcp.json

# Manual investigation scripts (raiz)
/simulate-hero-upload.js
/test-csv-validation.js
/test-hero.jpg

# catalog dev storage (seed committed separately)
/storage/catalog.json
/storage/store-settings.json
/storage/branding/

# QA smoke auxiliares locais (não versionados)
/scripts/qa/smoke-f0-prod.mjs
/scripts/qa/smoke-prod-netlify.mjs
/scripts/qa/smoke-prod-persistence-redeploy.mjs
/test-data/e2e/*.json
/test-data/e2e/debug-logo-out.webp
/test-data/e2e/logo-unit-local-header.png

# Multi-client registry operacional (secrets/refs reais)
/deploy/clients/clients.local.json
